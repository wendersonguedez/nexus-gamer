# 🎮 Nexus Gamer

> Vitrine de ofertas relâmpago focada no público gamer e entusiastas de hardware.

O projeto consiste em uma simulação de e-commerce (varejo) onde o usuário pode visualizar produtos em promoção, filtrar por categorias e simular a adição ao carrinho. O foco é a implementação de uma interface moderna, responsiva e performática.

## 🚀 Tecnologias

Este projeto foi desenvolvido utilizando as tecnologias mais modernas do ecossistema React:

-   **[Next.js 15](https://nextjs.org/)** (App Router & Server Components)
-   **[TypeScript](https://www.typescriptlang.org/)** (Tipagem estática e segurança)
-   **[Tailwind CSS](https://tailwindcss.com/)** (Estilização utilitária e responsiva)
-   **[pnpm](https://pnpm.io/)** (Gerenciador de pacotes performático)

## 📦 Funcionalidades (Escopo)

-   [ ] Listagem de produtos (Hardware e Periféricos).
-   [ ] Filtro dinâmico por categoria.
-   [ ] Consumo de API (Next.js API Routes).
-   [ ] Interface Responsiva (Mobile First).

## 🔧 Como rodar o projeto

### Pré-requisitos
Você precisa ter o **Node.js** instalado e o gerenciador de pacotes **pnpm**.

```bash
# Se não tiver o pnpm instalado:
npm install -g pnpm
```

Clone o repositório:

```bash
git clone https://github.com/wendersonguedez/nexus-gamer.git
```

Entre no diretório do projeto:

```bash
cd nexus-gamer
```

Instale as dependências:

```bash
pnpm install
```

Rode o servidor de desenvolvimento:
```bash
pnpm dev
```

Acesse no navegador: `http://localhost:3000`